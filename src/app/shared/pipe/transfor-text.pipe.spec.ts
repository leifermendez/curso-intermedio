import { TransforTextPipe } from './transfor-text.pipe';

describe('TransforTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TransforTextPipe();
    expect(pipe).toBeTruthy();
  });
});
